import { Container, Typography } from "@mui/material";

export default function ShippingNotFound(){
    return (
        <Container>
            <Typography variant="h3" component="h3">shipping not found</Typography>
        </Container>
    );
}